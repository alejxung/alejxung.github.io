/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/alejxung.github.io",
  output: "export",
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
